<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hyundae_futurenet.rocketddan.runners_hi.backend.model.mapper.feed.FeedFileMapper">

    <!--    <insert id="insertAll">-->
    <!--        INSERT ALL-->
    <!--        <foreach collection="filePathList" item="filePath" index="idx">-->
    <!--            <bind name="fileOrder" value="@java.lang.Integer@valueOf(idx + 1)"/>-->
    <!--            INTO FEED_FILE ( FEED_FILE_ID, FILE_PATH, FILE_ORDER, FEED_ID, CREATED_BY)-->
    <!--            VALUES (SEQ_FEED_FILE.NEXTVAL, #{filePath}, #{fileOrder}, #{feedId}, #{loginMemberId})-->
    <!--        </foreach>-->
    <!--        SELECT * FROM DUAL-->
    <!--    </insert>-->

</mapper>